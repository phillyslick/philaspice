<div id="content">
	<div id="modal">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <%= content_tag :h1, "Create a New Subcategory" %>
  </div>
  <div class="modal-body">
			<%= form_for([@category, @subcategory], url: category_subcategories_path) do |f| %>
			<%= f.label :name, "Name:" %>
			<%= f.text_field :name %>


		<%= f.submit "Create", class: 'btn-primary' %>
	<% end %>
		
  </div>
</div>
</div>