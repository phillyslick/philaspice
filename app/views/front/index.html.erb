<hr class="top">

<h2 class="main">Welcome</h2>

<div id="mainSlider" class="sliders">
	<ul class="bjqs">
		<% @sliders.each do |slider| %>
			<%= content_tag :li do %>
					<%= image_tag slider.picture.url(:main) %>
					<p><%= slider.name %></p>
			<% end %>
		<% end %>
	</ul>
</div>

<div id="featuredProduct" class="featuredSlice">
	<hr>
	<h2 class="featured"> Featured </h2>
	<%= image_tag @featured.front_hero.image.url(:medium) %>
	<h3><%= @featured.front_hero.name %></h3>
	<p><%= @featured.front_hero.description %></p>
</div>

<div id="catalogueFeature" class="featuredSlice">
	<hr>
	<h2 class="featured"> Our Catalogue </h2>
	<%= image_tag @option.image.url(:medium) %>
	<h3><%= @option.cat_heading %></h3>
	<p><%= @option.cat_info %></p>
</div>


<div id="quickCart">
	<%= render :partial => 'cart_table' %>
</div>