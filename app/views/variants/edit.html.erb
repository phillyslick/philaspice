<%= content_tag :h1, "Edit #{@variant.name}" %>

<%= form_for [@product, @variant] do |f| %>
	<%= f.label :name, "Name:" %>
	<%= f.text_field :name %>

	<%= f.label :price, "Price:" %>
	<%= f.text_field :price %>	
	
	<%= f.submit "Update" %>
<% end %>