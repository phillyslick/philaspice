<h1>Add A Variant to <%= @product.name %></h1>
<%= form_for [@product, @variant] do |f| %>
	<%= f.label :name, "Name:" %>
	<%= f.text_field :name %>

	<%= f.label :price, "Price:" %>
	<%= f.text_field :price %>	
	
	<%= f.submit "Create" %>
<% end %>